# ---------------------------------
# Common aliases
# ---------------------------------

alias home='cd ~'
alias dotfiles='cd $HOME/dotfiles'
alias projects= 'cd $HOME/Projects'

# ---------------------------------
# Git aliases
# ---------------------------------
alias g='git' 
